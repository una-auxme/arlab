# 4. Solution Strategy
<!-- Summary of the fundamental decisions and solution strategies that shape the architecture/software. Can include technology, top-level decomposition, approaches to achieve top quality goals and relevant organizational decisions. -->

TODO: Keep the explanation of these key decisions short. Motivate what you have decided and why you decided that way, based upon your problem statement, the quality goals and key constraints. Refer to details in the following sections.
Can be written as a short list and extended with links to the other sections for details.

HRI (Currently only planned):

AI based libraries for flexible natural interaction

- Voice activity detection + llm based relevance checks instead of Wakeword detection
    --> Open source Wakeword detection is often unreliable (Robot does not react), but might provide less false positives.
- LLM with tool support provides a flexible and easily extensible human-robot-interface
    --> Allows for natural language flow that static StateMachines or similar, but is also more difficult to control/can lead to unexpected outputs
- AI based TextToSpeech converts generated text into natural language

Decision Making:

- Behavior tree
    --> Relatively simple and effective for fixed tasks
    --> Direct integration of ROS actions with behaviors

Manipulation:

- The orchestrator sends driving and pose commands to MoveIt2 via a publisher/subscriber model
    --> Reason: enables loose coupling between high-level task logic and low-level motion planning, better testing

- MoveIt2 is used for path and motion planning
    --> Reason: well-established framework for collision-aware planning, inverse kinematics, and trajectory generation

- Grip force is determined based on object classification
    --> Reason: Object types are used to adapt grip strength dynamically, reduces the risk of damage or failed grasps
    --> local in manipulation because only the manipulator needs it

- Simulation-based development & validation
    --> All manipulation strategies are first tested in simulation (e.g. Gazebo) because the real robot wasn't ready

Knowledge base:

- Relationale Database
    --> Well-known and proven, Little to no semantic required.
- Sql-Alchemy
    --> "Easy" to use,  popular and proven python library, Async Api, has an ORM with support for Python classes
    --> Database schema defined via python classes
    --> Conversion functions between python classes and ROS messages for the provided ROS services
- Async service callbacks
    --> Allows multiple simultaneous service requests without the need for additional threads
    --> It uses a custom ROS executor that has been integrated with asyncio to improve throughput. (The default and MultiThreaded ROS executors do not support asyncio based futures)
More information available in DR3(<https://uniaugsburg-my.sharepoint.com/:p:/g/personal/lukas_asam_uni-a_de/EYPY4IcXzF9Jh1sn1inTfGMBxD56mWEq516a2mqELa7Q7Q?rtime=ZvVJH13I3Ug>)

Movement:

- Orchestrator sends movement instructions to the according nodes (e.g. nav2 or SLAM) -> orchestrator provides a single interface for the global decision making (splitting commands into high level & low level)
- nav2 in conjunction with SLAM is established and has proven to be reliable in the past
- Testing in simulation since hardware wasn't available at the time

Safety:

Decision Making for a Hybrid Architecture of Decentralized Safety Submodules and a Central Safety Node

- **Scalability & Team Efficiency**
  - *Problem:* We cannot afford a dedicated safety team, and external enforcement would slow down development.
  - *Solution:* By enabling each team to implement safety locally by serving templates, we integrate safety directly into the development process without extra overhead.

- **Simplicity & Adaptability**
  - *Problem:* Centralized safety logic would become complex and hard to adapt as features evolve.
  - *Solution:* Local safety submodules with simple templates keep safety close to the local functionality and local paramaters. This allows quick adjustments.

- **Error Transparency & Consistent Decisions**
  - *Problem:* Without structured aggregation, local errors remain hidden and handling becomes inconsistent.
  - *Solution:* The central safety node collects all submodule states, evaluates severity, and ensures uniform reactions across the system.

- **Reliability & Compliance**
  - *Problem:* Failures in local modules could compromise overall safety and make audits difficult.
  - *Solution:* Central reporting ensures robustness, provides reliable monitoring, and creates a single point of truth for audits and certification.


# Computer Vision:

- Decision Making for a object detection model. Comparison between the general image processing module OpenCV and Yolo specialized on real-time detection:

YOLO (Deep Learning)
**Pros:**
- High accuracy across many object classes  
- Real-time capable on modern GPU/edge hardware (e.g., Jetson)  
- Handles complex scenes and small objects well  
- Large community, many pre-trained models available  

**Cons:**
- Requires GPU or strong hardware for good performance  
- Training/customization can be resource-intensive  
- Higher power consumption → less ideal for ultra-low-power devices  

OpenCV
**Pros:**
- Runs on low-power CPUs (Raspberry Pi, Arduino-like boards)  
- Easy setup, minimal training data needed  
- Energy-efficient, good for simple/specific objects (e.g., faces, hands)  

**Cons:**
- Low accuracy and robustness in complex environments  
- Poor scalability with multiple objects at once  
- Limited flexibility → struggles with custom objects  
- Outdated compared to modern deep learning approaches  

Summary
- **YOLO** = best choice for a versatile home-assistant robot that needs to detect many real-world objects in real time.  
- **OpenCV** = only reasonable if constrained to very weak hardware and very simple detection tasks.  

Decision: Based on our powerful Jetson Hardware we will try the approach with the powerful Yolo Network. It is easy scale (Nano, Small, Medium, Large), easy to train based on an existing model (COCO dataset) and easy to imlpement. First tests have been successful with the YOLO 11 NANO model. 

---

Training dataset:
Regarding to our specific task handling groceries further training will be necessary. Training with opensource grocery datasets from the kaggle platform is planned.

Validation:
Tests including the Jetson Nano Orin and the Intelsense Camera SR305 showed hardware issues with the camera and were leading to a temporary solution using the Azure Kinect DK 1880. For the long term the project aims to use the Intelsense Camera.
Besides the negative outcome with the camera, the YOLO 11 Nano network ran smoothly on the Jetson Nano and showed positive first grocery detection tests. 

Make detected obect data accessable:
To ensure that every software module can access the required object data computer vision uses the services of the knowledge base. Strict seperation of concerns between the robot modules lead to a central knowledge base as a data sharing module. Extracting specific data with the object detection module and saving it in the knowledge base, solves the accessability issue.

# Perception:
Installing and testing hardware drivers for the Intelsense Camera and the LiDAR.
Tests showed incompatiblities between Intelsense Camera drivers and python version. As a temporary solution we switched to the Azure Kinect camera which supports actua python versions and libraries.
