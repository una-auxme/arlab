# 4. Building Block View

## Whitebox Overall System - Level 1

![Whitebox Overall System](./images/Building%20Block%20View%20Diagrams/GesamtBlackboxes.svg)

**Rationale:** We used functional decomposition to separate responsibilities:

- Human Robot Interaction (HRI)
- Global Decision Making
- Manipulation
- Movement
- Computer Vision
- Perception
- Knowledgebase

**Contained Building Blocks:**

| **Building Block**     | **Description**                                                                                          |
|------------------------|----------------------------------------------------------------------------------------------------------|
| HRI                    | Used to interact with the robot                                                                          |
| Global Decision Making | Receives task from HRI; initiates the required steps to fullfill the given task                          |
| Manipulation           | Handles all tasks related to the manipulator and the gripper                                             |
| Movement               | Handles all tasks related to orientation and moving the robot in the environment                         |
| Computer Vision        | Handles object/human detection                                                                           |
| Perception             | Handles LiDAR and Realsense2 camera, aswell as physical input devices such as buttons and microphone     |
| Knowledge Base         | Stores and manages data such as maps and locations (movement) or suitable gripping forces (manipulation) |

## Building Blocks

**The following detailed levels will be building block specific.**

### HRI - Level 2

![HRI Blackbox](./images/Building%20Block%20View%20Diagrams/HRIBlackboxes.svg)

**Contained Building Blocks:**

| **Building Block** | **Description**                                                                           |
|--------------------|-------------------------------------------------------------------------------------------|
| VizBox             | Tool provided by RoboCup to visualize the robots "thoughts" and actions to the spectators |
| Speech Recognition | Translates spoken text to a command thats usable by nodes                                 |

### HRI - Level 3

![HRI Whitebox](./images/Building%20Block%20View%20Diagrams/HRIWhiteboxes.svg)

**Contained Building Blocks:**

| **Building Block** | **Description**                    |
|--------------------|------------------------------------|
| Speech to Text     | Translates spoken text to a string |
| LLM Server         | Serves the LLM Chat                |
| LLM Chat           | Translates text to a GlobalCommand |
| Text to Speech     | Gives a spoken answer to the user  |

### Global Decision Making - Level 2

![Global Decision Making Blackbox](./images/Building%20Block%20View%20Diagrams/GlobalDecisionMakingWhiteboxes.svg)

**Contained Building Blocks:**

| **Building Block**             | **Description**                         |
|--------------------------------|-----------------------------------------|
| Behaviour Tree                 | Chooses actions based on behaviour tree |
| Safety: Global Decision Making | Checks for alive signal of the node     |

Because of the simple structure and current state of development, no further decomposition is required for this building block.

### Manipulation - Level 2

![Manipulation Blackbox](./images/Building%20Block%20View%20Diagrams/ManipulationBlackboxes.svg)

**Contained Building Blocks:**

| **Building Block**         | **Description**                                                                                                                          |
|----------------------------|------------------------------------------------------------------------------------------------------------------------------------------|
| Orchestrator               | Coordinates the general tasks of manipulation                                                                                            |
| Gripping Parameter Service | Returns gripping parameters for a given object: gripping force, grip type, grip position and orientation mode, object weight             |
| Processing                 | Calculation and planning of jobs, motions and paths; activation/deactivation of force sensors and force monitor                          |
| Acting                     | Execution of planned movements on the robot arm and gripper                                                                              |
| Force Monitor              | Monitoring of hand force sensors during the manipulaiton process to detect dropped objects                                               |
| Safety                     | Checks for node health; surveils gripping force; surveils surroundings of manipulator; surveils velocity and acceleration of manipulator |

### Manipulation - Level 3

![Manipulation Whitebox](./images/Building%20Block%20View%20Diagrams/ManipulationWhiteboxes.svg)

**Contained Building Blocks (Orchestrator):**

| **Building Block**              | **Description**                                                                |
|---------------------------------|--------------------------------------------------------------------------------|
| Command Splitting               | Divides the joint ManipulationAction for the specific jobs                     |
| Call gripping parameter service | Calls the service to receive the gripping parameters for a given object        |
| Calculate Grasping Point        | Calculates an optimal grasping point for the gripper                           |
| Calculate Placing Point         | Calculates an optimal placing point for an object                              |
| Data Publisher                  | Publishes the data to MoveIt                                                   |

**Contained Building Blocks (Processing):**

| **Building Block**           | **Description**                                                                                                                         |
|------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------|
| Orchestrator Listener        | Subscribes to data published by the orchestrator                                                                                        |
| Job Runner                   | Runs a process or function based on a received command from orchestrator listener; activates/deactivates force sensors and force monitor|
| Plan and Execute             | Plans trajectories and provides execute signals for acting and the force monitoring                                                     |
| MoveIt Init                  | Initializes Move Groub Interfaces for MoveIt                                                                                            |

The hardware interfaces of the manipulator and the gripper are controlled by the **Processing**-Nodes **Plan and Execute**-Functions

### Movement - Level 2

![Movement Blackbox](./images/Building%20Block%20View%20Diagrams/MovementBlackboxes.svg)

**Contained Building Blocks:**

| **Building Block** | **Description**                                                                                                  |
|--------------------|------------------------------------------------------------------------------------------------------------------|
| Orchestrator       | Coordinates the general tasks of movement; controls the Navigation Stack and triggers auto-annotation of the map |
| Navigation Stack   | Handles localization, mapping and moving to a specific location on a given map                                   |
| Entity Placer      | Adds entities with a manually selected pose to the knowledge base, used for semantic annotation of the map       |

The Orchestrator uses the snapshot action of Computer Vision to annotate the map with entities, which Computer Vision writes into the Knowledge Base.
The Entity Placer writes directly into the Knowledge Base and is the fallback if the automatic annotation fails.

### Movement - Level 3

![Movement Whitebox](./images/Building%20Block%20View%20Diagrams/MovementWhiteboxes.svg)

**Contained Building Blocks (Orchestrator):**

| **Building Block** | **Description**                                                                                                                                |
|--------------------|------------------------------------------------------------------------------------------------------------------------------------------------|
| Action Server      | Accepts MovementAction goals on `/movement/action` (`cmd`, `enable`) and forwards them to the matching job; returns error code and message     |
| Process Manager    | Starts and stops localization (AMCL, currently not used), mapping (SLAM Toolbox) and navigation (Nav2) as separate processes                   |
| Map Saving         | Saves the latest map to file and optionally to the Knowledge Base (`/arlab/knowledge/add_map`)                                                 |
| Auto-Annotation    | Checks odometry for snapshot conditions and sends goals to `/vision/snapshot`; publishes on `/arlab/movement/annotating` while a snapshot runs |

Auto-annotation takes a snapshot only if the robot is moving slowly, a minimum time has elapsed and the robot has moved or turned enough since the last snapshot.
The thresholds are set as parameters in `arlab_navigation_params.yaml`.

**Contained Building Blocks (Navigation Stack):**

| **Building Block** | **Description**                                                    |
|--------------------|--------------------------------------------------------------------|
| Navigation         | Handles path planning and publishes velocity commands to the robot |
| Localization       | Handles localization and orienting on a given map                  |
| SLAM Toolbox       | Handles map generating and updating                                |

**Contained Building Blocks (Entity Placer):**

| **Building Block** | **Description**                                                                                            |
|--------------------|------------------------------------------------------------------------------------------------------------|
| Frame Transform    | Transforms the pose selected in RViz on `/arlab/entity_pose` into the target frame (default: `map`) via TF |
| Pose Staging       | Stages the latest transformed pose; a new selection replaces the staged pose                               |
| Place Service      | Offers `/arlab/entity_placer/place` (extends AddEntity.srv) and adds the entity to the Knowledge Base      |

The mapping drive (`mapping_drive.launch.py`) starts the Orchestrator, Object Detection and the Knowledge Base, then enables mapping and auto-annotation,
so the operator only has to drive the robot.

### Computer Vision - Level 2

![Computer Vision](./images/Building%20Block%20View%20Diagrams/ComputerVisionWhiteboxes.svg)

**Contained Building Blocks:**

| **Building Block** | **Description** |
|--------------------|-----------------|
| Snapshot Action Server | Accepts goals on `/vision/snapshot`; frames are only processed while a goal is active |
| Model Registry | Loads the YOLO models from `models.yaml`; pinned models stay in memory, others are released after an idle timeout |
| Segmentation and Classification (YOLO) | Runs the selected models on the 2D RGB image, providing classes and segmentation masks |
| Geometry and Entity Mapping | Filters and clusters the point cloud per mask, and maps the label to an entity via `labels.yaml` |
| Knowledge Base Client | Adds the new entities and deletes the pickables they supersede |

All of these live in the single `ObjectDetection` node, so no further decomposition is necessary.

Computer Vision has no local safety node yet, so its health is not reported to the central Safety Node.

### Perception - Level 2

![Perception](./images/Building%20Block%20View%20Diagrams/PerceptionWhiteboxes.svg)

**Contained Building Blocks:**

| **Building Block**                 | **Description**                                                                          |
|------------------------------------|------------------------------------------------------------------------------------------|
| LiDAR Node                         | Receives unfiltered LiDAR data. Publishes data as LaserScan to the ros2 system           |
| Realsense2 Camera Node             | Receives RGB camera data. Publishes RGB data as ros2 image to the ros2 system            |
| Emergency Stop                     | Emergency stop that immediately shuts down all tasks and processes and halts the robot   |
| Microphone/Text Input/Button Input | Diverse input methods to interact with the robot                                         |
| Safety                             | Checks for node health                                                                   |

Due to the low level components contained in this building block, no further decomposition is necessary.

### Knowledge Base - Level 2

![Knowledge Base](./images/Building%20Block%20View%20Diagrams/KnowledgebaseWhiteboxes.svg)

**Contained Building Blocks:**

| **Building Block** | **Description**                                                 |
|--------------------|-----------------------------------------------------------------|
| ROS service clients| Represents different part of the system that can request data   |
| Database Node      | Handles ROS requests and uses SQLAlchemy to manage the database |
| PostgresSQL        | The underlying database system                                  |
| Safety             | Checks for node health                                          |

### Safety - Level 2

Due to the simple structure of this building block, no further decomposition is necessary. However here follows a brief description of the key concept.

| **Building Block** | **Description**                                                                                                                                                                                                                                                                                         |
|--------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Global Safety Node | This Node is a central control. It is set hierarchical on top of the local safety nodes and has the ability to freeze the system for specific safety critical situations. Additionally this node scans the health state of the system modules by subscribing to the heartbeats of the local safety nodes|
| Local Safety Nodes | These nodes are located inside the modules among the system and are adapted to scan the local tasks for safety issues as described in the chapters above. In case of a system critical issue, the local safety nodes will communicate this occurancy to the central safety node                         |
